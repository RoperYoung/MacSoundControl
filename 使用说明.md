# MacSoundControl 1.0.0

这是一个驻留在 macOS 菜单栏的音频快捷控制工具。它可以切换系统默认输入和输出、调节系统与各软件的输出音量、按开关保持当前麦克风在线，并实时显示当前麦克风的输入电平。

Finder、DMG 安装窗口与 About 使用统一的橙红渐变扬声器和三滑杆产品图标。主界面侧栏不重复显示大品牌图标；菜单栏为了在 17 pt 下保持清晰，继续使用独立的单色系统模板图标，并自动跟随 macOS 的浅色、深色和悬停状态。

## 使用

1. 打开“MacSoundControl.app”。
2. 点击菜单栏的无外圈扬声器与双声波图标；它使用 macOS 系统颜色，会自动适配浅色、深色和悬停状态。
3. 在“选择系统输出”或“选择系统输入”标题下直接点击设备；整个菜单没有下级菜单。每行会按设备类型显示耳机、扬声器、显示器、电脑、手机、麦克风或虚拟波形图标，蓝色圆形图标就是 macOS 当前真实的默认设备。切换后面板会保持打开，可以继续设置其他项目。
4. 面板不再重复显示标题、当前输入/输出和运行状态摘要，打开后第一项就是“系统输出音量”；拖动滑杆调节当前输出，点击左侧扬声器可静音或取消静音。完整设备与运行状态仍可从菜单栏图标的悬浮提示查看。
5. 在“应用音量”右侧选择“统一控制”或“分开控制”。统一控制只保留系统总音量；分开控制会列出当前已向 CoreAudio 注册音频输出的应用，即使它此刻没有发出声音，也可以预先设置独立音量。首次把某个软件调到 100% 以外时，请允许 macOS 的“系统音频录制”权限。
6. 点击“声音输入测试”，应用只会打开主界面并定位到连续页面中的“声音测试”区段，不会自动占用麦克风，也不会再出现第二个测试窗口。
7. 在“声音测试”区段使用“切换输入源”选择要检查的系统输入，然后点击中央的“启动测试”。对着麦克风说话时，顶部一行会同步显示百分比和模拟 `dBFS`，横向排列的 36 根等高细条会按音量从灰色逐根点亮为系统蓝；点击同一操作行里的“停止测试”或关闭主窗口即可结束测试。
8. 点击“开启麦克风常驻”切换常驻状态；菜单项前有勾时表示已开启。点击这个开关或“展示虚拟设备”后面板也会保持打开。
9. 点击底部“打开主界面”进入扁平原生控制台。右侧是一张从系统输出音量连续滚动到 About 的完整页面；左侧七个选项用于定位对应区段，手动滚动时选中项也会同步变化。
10. 在 About 区段点击“检查更新”可立即启动 Sparkle 更新流程。应用也会定期读取 GitHub Pages 上的静态更新源；发现新版时由系统原生界面让你决定是否下载安装，默认不会静默自动安装。

## 菜单功能

- **系统输出音量**：直接控制当前默认主要输出的系统音量，支持静音。拖动时滑块和百分比在本地立即跟随，并使用已缓存的当前设备直接写入 CoreAudio；本工具自身触发的系统通知不会在拖动中把旧值写回，松手后再校准一次真实系统音量。整个控制区在面板内使用独立的对称边距，不为下方设备图标列预留空位。HDMI、DisplayPort 或其他由外部硬件控制音量的设备会显示为不可调节。
- **应用音量**：右侧使用 macOS 原生双分段控件选择“统一控制 / 分开控制”，点击后菜单保持打开。“统一控制”隐藏软件列表并释放独立音频处理通道，只由上方系统输出音量调节；“分开控制”列出当前已向 macOS CoreAudio 注册音频输出、且能映射到常规 App 的软件。每个软件以 Logo、限定宽度的单行名称、原生滑杆和百分比横向排列；过长名称会在末尾省略，悬浮时仍可查看完整名称。列表不再要求软件此刻正在播放，因此可以在发声前预设音量。0% 相当于静音，100% 为原始音量；设置按软件保存在本机，切换统一控制不会删除，软件下次注册音频通道时也会恢复。分开控制需要 macOS 15 或更高版本。
- **选择系统输出**：在 460 pt 宽的主菜单中以 42 pt 原生行直接列出可作为默认输出的设备；左侧 30 pt SF Symbol 圆形图标由传输类型和设备名称识别，14 pt 设备名右侧显示 12.5 pt 的“蓝牙 / USB / 内建 / HDMI / 虚拟”等连接方式。切换输出不会改变系统输入，完成后面板保持打开并立即更新蓝色选中图标。
- **选择系统输入**：使用与输出相同的间距和图标规则，识别手机连续互通、耳机麦克风、电脑麦克风、普通麦克风和虚拟设备；切换后常驻会自动跟随新的系统输入，面板保持打开并立即更新选中状态。
- **声音输入测试**：打开主界面并定位到“声音测试”锚点，但不自动开始。区段顶部可用“切换输入源”直接切换系统默认输入；未测试时，中央只显示“启动测试”。开始后，标题、百分比和模拟 `dBFS` 合并在顶部汇总行，横向排列的 36 根等高细竖条显示电平，状态和停止/重试操作共用底部一行；已激活部分为系统蓝，未激活部分为动态中性灰，不记录或绘制高低起伏的历史波形。每批音频到达后直接更新，不使用显示定时器、上升/回落平滑或旧值补帧，因此数值可能比平滑电平条更容易跳动。测试没有固定时长，也不会生成“检测到声音”的结果提示。
- **重新打开当前输入通道**：在主窗口“常驻与启动”区段中随常驻可用状态显示；常驻开启但输入异常时，手动重建当前麦克风通道。
- **开启麦克风常驻**：持续保持当前系统输入通道在线，适用于蓝牙、USB、内建及其他可用麦克风。关闭时不会恢复之前的系统输入；点击只更新勾选和运行状态，不关闭面板。
- **展示虚拟设备**：默认不勾选，同时隐藏输入和输出列表中的虚拟音频设备；勾选或取消后会在当前面板中立即显示或隐藏，面板不会关闭。
- **开机自动启动**：使用与常驻、虚拟设备相同的前置勾选和文字列；点击后菜单保持打开。开启后让应用随 macOS 登录启动；系统要求批准时会跳转到登录项设置。
- 上述三个状态开关都保持简洁的“系统勾选框 + 文字”形式，使用相同的 30 pt 行高和 13.5 pt 字号，不增加装饰图标。
- **打开主界面**：打开或唤起同一个 960 × 700 pt 原生 AppKit 窗口。右侧连续页面与菜单共享同一业务动作，可实际操作系统输出音量/静音、应用音量模式和各软件音量、输入/输出、常驻/虚拟设备/开机启动、输入重开、权限和声音测试；About 区段显示真实 Bundle 版本、最低系统、隐私说明、GitHub 仓库、Sparkle 检查更新和第三方许可。

## 主界面

- 主窗口采用参考项目 / Telegram 式内嵌侧栏结构：整窗先铺设系统纯色底板，左侧是一张浅灰圆角导航卡片嵌在窗口内，不再是左右分开的贯穿式侧栏；右侧内容和侧栏卡片共享同一个窗口背景，不额外绘制竖线，也不会在窗口底部透出桌面。
- 右侧只有一个 `NSScrollView` 和一张连续页面，不再切换“音频概览 / 声音输入测试 / 关于”三张独立页面。七个区段依次为“系统输出音量 / 应用音量 / 选择系统输出 / 选择系统输入 / 常驻与启动 / 声音测试 / 关于”；左侧点击直接定位锚点，用户手动滚动时侧栏选中态同步跟随。
- 每个一级区段末尾保留低对比动态分隔线，并在下一区段前保留约 52 pt 的结构留白，不再依赖圆角卡片分组。设备行、应用模式、三个状态开关和运行状态均使用透明背景；当前设备只通过系统蓝图标、semibold 名称和“当前”标识表达，不用整行灰底或蓝底。
- “系统输出音量”使用横向系统滑杆，左侧为小音量、右侧为大音量；上方显示当前输出和百分比或静音状态，下方保留独立静音按钮。主界面与菜单共用同一套拖动状态：按住滑块时只更新当前控件，不再为每一个 CoreAudio 通知重建整页，松手后再统一校准真实系统值。
- 应用数量可能很多，因此每个软件仍采用同一行的 Logo、名称、横向滑杆和百分比。应用滑杆同样在拖动期间暂停整页重建，松手后统一刷新。
- “声音测试”锚点进入时保持空闲，不会自动打开采集；顶部不再显示重复的测试说明，空闲启动按钮保持居中。测试中数值汇总为一行，状态和操作也合并为一行，减少竖向堆叠。36 根细条高度始终一致，只用灰色与系统蓝表达未激活/已激活进度；它不是保存历史的示波器波形。百分比用于快速观察相对大小，模拟 `dBFS` 用于显示数字音频电平。

左侧七个锚点按钮共用固定的图标列和标题列，选中背景覆盖完整导航行；选中项使用实心系统蓝底白字，未选中项保持透明。About 作为连续页面的最后一个区段，不再使用营销说明、代码或盾牌等装饰符号，而以真实图标、版本、“本地运行 / 系统要求 / 开源计划”三行透明信息和轻分隔线呈现。

主窗口七个分区标题统一使用 17 pt semibold 系统字体和动态标签颜色；应用音量的“统一控制 / 分开控制”与标题位于同一行，旧说明“分别保存并控制每个应用的音量”不再显示。菜单中的四个分区标题继续使用原有 14 pt semibold 规则。

About 中的“开源计划”直接链接到 [RoperYoung/MacSoundControl](https://github.com/RoperYoung/MacSoundControl)。点击“检查更新”时，应用把操作交给 Sparkle；Sparkle 从 `roperyoung.github.io/MacSoundControl/appcast.xml` 读取正式版本，验证 GitHub Release ZIP 的 EdDSA 签名和 App 的 Developer ID 后再执行更新。推荐标签格式为 `vX.Y.Z`。整个项目的许可证仍需正式确认；`THIRD_PARTY_NOTICES.md` 同时记录 MacMix 与 Sparkle 的来源和许可。

如果常驻已关闭，声音输入测试只会临时打开麦克风，点击“停止测试”或关闭主窗口后会释放输入通道。如果常驻已开启，测试会复用现有通道，不会再打开第二个采集会话。

## 隐私与资源使用

- 音频缓冲只用于计算当前输入电平，会被立即丢弃；应用不录制文件、不播放、不上传，也不进行语音识别。
- Sparkle 会定期进行只读更新检查，也可由用户手动触发；它只访问 GitHub Pages 静态 appcast 和用户确认下载的 GitHub Release 文件，没有遥测或自建服务，音频和本机设置不会随请求发送。
- 声音测试区段中的模拟分贝是数字音频电平 `dBFS`：`0 dBFS` 代表数字峰值，显示下限为 `-60 dBFS`。它不是需要专业校准的环境声压级 `dB SPL`，不能替代声级计。
- 分开控制使用 macOS Audio Process Tap 在内存中实时缩放所选软件的输出，再送到当前系统输出；它同样不录制、不保存、不上传音频。只有某个软件不是 100% 时才创建处理通道；切到统一控制时会先恢复原始增益并释放全部独立处理通道。
- macOS 要求处理其他软件的声音前取得“系统音频录制”权限。拒绝权限不会影响系统总音量、设备切换、麦克风常驻或声音输入测试，只会使分应用音量无法生效。
- 常驻开启或实时测试进行时，macOS 会显示麦克风正在使用，这是正常现象。
- 长期开启常驻会略微增加 Mac 和无线麦克风的耗电。
- 蓝牙音质与响应速度仍受 HFP、麦克风固件 DSP 和无线传输影响；声音测试区段无法提前显示尚未送到 macOS 的音频。需要更低延迟或更高质量时建议使用接收器的 USB 模式。

## 应用兼容性

系统听写以及选择“系统默认”的应用会跟随菜单中的输入切换。微信、腾讯会议、Zoom 等软件如果在自己的设置里固定了某个输入或输出设备，可能不会跟随；将对应来源改成“系统默认”即可。

菜单音量滑杆控制的是 macOS 默认主要输出。显示器、扩音器或耳机如果只允许通过自身按键调节音量，应用会保留设备切换能力并禁用滑杆。

分应用音量显示 macOS CoreAudio 当前已注册、并且能映射到常规 App 的音频进程，不再以“正在播放”为条件；因此暂停播放后，只要软件仍保留音频进程对象，就会继续留在列表中。少数软件只有首次播放时才向系统注册音频对象，这类软件会在第一次发声后出现，随后按 Bundle ID 恢复之前保存的音量。使用独占设备、特殊驱动或非标准音频链路的软件仍可能不会出现。分应用音量和系统总音量会同时作用：例如软件为 50%、系统总音量也为 50% 时，最终听感会继续降低。

设备断开时，工具会跟随 macOS 的实际回退设备；设备重新出现后只刷新列表，不会擅自抢回。系统提示音使用 macOS 单独的“声音效果播放设备”设置，本工具只切换主要系统输出。
